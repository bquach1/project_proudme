'use strict';

const clone = require('../helpers/clone');

class RemoveOptions {
  constructor(obj) {
    if (obj == null) {
      return;
***REMOVED***
    Object.assign(this, clone(obj));
***REMOVED***
}

module.exports = RemoveOptions;
